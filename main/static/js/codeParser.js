

function parseCode(code) {
  let firstWord = code.split(' ')[0];
  
  if (firstWord === 'Box') {
  
  } else if (firstWord === 'Cross') {
    let firstParam = code.split(' ')[0];
    let secondParam = code.split(' ')[1]; 
    let thirdParam = code.split(' ')[1]; 
    if (firstParam === 'X') {
      let x = secondParm;
    }
  } else if (firstWord === 'Thread_Up') {
  
  } else if (firstWord === 'Thread_Down'){
  
  }
}

